class DeviceActiveModel{
  String deviceName, roomName, imageAsset, unit, value;

  bool isSwitchOn;

  DeviceActiveModel(this.deviceName, this.roomName, this.imageAsset, this.unit,
      this.value, this.isSwitchOn);
}